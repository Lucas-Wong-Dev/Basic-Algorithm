// 数组正负值排序
// 暂时将0算为正数
// 插入排序法
#include <iostream>
using namespace std;
int main()
{
    int array = {1, -2, -4, 5, 9, -3, -8, 6};
    int len = sizeof(array)/sizeof(array[0]);
    int index = 0;
    for(int i = 0;i < len;i ++){
        for(int j = i;j > index;j --){
            swap(array[])
        }
    }
    return 0;
}